Go utilities.

dcache/
	Delayed cache as a self-populating ring buffer.

fcache/
	Timed file cache which refreshes only after a certain duration.

fcmp/
	Compare files quickly.

pool/
	Simple, versatile worker pool implementation.

splay/
	Splayed file tree.

table/
	Tabular indexing and insertion of fixed-width rows using Splay.

tcache/
	Timed in-memory cache which refreshes only after a certain duration.

uuid/
	Generate cryptographically-random UUIDs (128 bits) or MegaUUIDs (1028
	bits).
